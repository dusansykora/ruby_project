<p>
  <strong>Name:</strong>
  <%= @band.name %>
</p>

<p>
  <strong>Establish year:</strong>
  <%= @band.establish_year %>
</p>

<p>
  <strong>Genre:</strong>
  <%= @band.genre.name %>
</p>

<p>
  <strong>Members:</strong>
  <%= @members %>
</p>
<p>
  <%= image_tag(@band.cover_photo.url(:thumb)) %>
</p>

<%= link_to 'Edit', edit_band_path(@band) %> |
<%= link_to 'Delete', band_path(@band), method: :delete, data: { confirm: 'Delete this band?' } %> |
<%= link_to 'Back', bands_path %> <br>
<%= link_to 'New Event', new_band_event_path(@band) %> |
<%= link_to 'Show Events', band_events_path(@band) %> <br>
<%= link_to 'New Opinion', new_band_opinion_path(@band) %> |
<%= link_to 'Show Opinions', band_opinions_path(@band) %> <br>
<%= link_to 'New Post', new_band_post_path(@band) %> |
<%= link_to 'Show Posts', band_posts_path(@band) %> <br>
<%= link_to 'New Album', new_band_album_path(@band) %> |
<%= link_to 'Show Albums', band_albums_path(@band) %>
